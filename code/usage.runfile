title  'Developer Tools'

usage  'deploy [MESSAGE --production]'
option '--production', 'Deploy to production instead of stage'
action 'deploy' do |args|
  puts args['MESSAGE']
  
  if args['--production']
    puts 'Deploying to production'
  else
    puts 'Deploying to stage'
  end
end